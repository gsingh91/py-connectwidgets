---
title: "Testing connectwidgets"
format:
  html:
    code-fold: true
jupyter: python3
---

```{python}
import connectwidgets
from dotenv import load_dotenv
load_dotenv()
import os

c = connectwidgets.connect(os.getenv("CONNECT_SERVER"),os.getenv("CONNECT_API_KEY"))
print(c)
#c.get_content()
df = c.get_content()
connectwidgets.rsc_table(df)
```